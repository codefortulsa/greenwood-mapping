---
description: Restart Docker containers
---

Restart Docker containers. If the user specifies a service name, restart only that service, otherwise restart all services.

Use: `docker compose restart <service?>`
