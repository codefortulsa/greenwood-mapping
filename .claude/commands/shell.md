---
description: Open Django shell_plus in the container
---

Open Django shell_plus (requires `django-extensions`, already in pyproject):
`docker compose run --rm app poetry run python manage.py shell_plus`
