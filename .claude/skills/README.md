# Project skills

Per-project Claude Code skills developed for greenwood-mapping live here.
Each skill is its own subdirectory with a `SKILL.md` describing when to
trigger it, followed by any scripts/templates it needs.

None yet.
